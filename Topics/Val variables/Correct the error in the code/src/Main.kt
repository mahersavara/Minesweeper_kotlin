fun main() {
     val CONST: Int
    var listNumber = mutableListOf<Int>()
    CONST = 100
    listNumber.add(CONST * CONST)
    listNumber.add(CONST * CONST * CONST)
    listNumber.add(CONST * CONST * CONST * CONST)
    println(listNumber)
    val text: String = "hello, world"
    val number:Int = 3
    var gree: Int
// -> cần có type cho gree !
}